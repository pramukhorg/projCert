node('TestServer'){
   stage('puppetinstallation') {
      // install puppet agent
      sh "apt-get update"
      sh "apt-get install -yq puppet"
   }
   stage('puppetagent') {
      // Run puppet agent
      sh "/usr/bin/puppet agent -t"
   }
}
